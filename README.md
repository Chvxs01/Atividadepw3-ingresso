# pw3-ingresso

Felipe Santos Souza e Gabriel Chaves

CREATE TABLE TBL_SESSAO (
ID_SESSAO BIGINT PRIMARY KEY IDENTITY,
DIA DATE,
HORARIO TIME,
PRECO FLOAT,
ID_FILME BIGINT,
SALA VARCHAR(300),
FOREIGN KEY (ID_FILME) REFERENCES TBL_FILME(ID_FILME)
);